{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build",
    "bower": "bower"
  },
  "devDependencies": {
    "pulp": "^9.0.1",
    "purescript": "^0.9.1",
    "rimraf": "^2.5.3",
    "bower": "^1.7.1"
  }
}
